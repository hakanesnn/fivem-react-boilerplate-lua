import React, { useState } from 'react';
import './App.css';
import { debugData } from '../utils/debugData';

debugData([
	{
		action: 'setVisible',
		data: true,
	},
]);

const App: React.FC = () => {
	return <div></div>;
};

export default App;
